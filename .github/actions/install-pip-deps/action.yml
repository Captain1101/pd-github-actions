name: "Install pip dependencies"
description: "what name says"
runs:
  using: composite
  steps:
    - name: Print step name
      run: |
        echo "Installing all required depdendencies.."
        "pip install -r requirements.txt"
      working-directory: deps/python-greetings
      shell: cmd
